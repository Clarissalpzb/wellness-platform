services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: wellness
      POSTGRES_PASSWORD: wellness_dev
      POSTGRES_DB: wellness_platform
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
